#!/bin/bash 

cat <<- EOF 

Django is a high level web framework written in Python. Django is the Python equivalent of Ruby on Rails. If you want to build large production ready web applications in Python then look no further than Django.
    
EOF