#!/bin/bash 

cat <<- EOF 

Ruby on Rails is a server-side web application which implements the server side MVC pattern. It is written in Ruby which offers the same easy to read syntax you would expect working with Ruby.
EOF