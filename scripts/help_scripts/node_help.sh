#!/bin/bash 

printf "\n \n Node.js® is a JavaScript runtime built on Chrome's V8 JavaScript engine. \n If you are going to work with JavaScript it is probably a good idea to have node installe \n There are some frameworks that rely on having node installed such as Rails and Phoenix \n so it is probably a good idea to have node installed. You also will have the option to install node version manager \n which lets \n you switch between different versions of node and install dependencies for each separate version of node \n \n"